----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
##START git init
	
	git init

##END git init
--------------------------------------------------

--------------------------------------------------
##START git config

	git config --global user.name "Kishangopal"
	git config --global user.email "kishangopal@email.com"
	git config --list 

##END git config
--------------------------------------------------

--------------------------------------------------
##START git diff

	git diff hash1 hash2 hash3
	git diff hash1..hash2
	git diff hash1...hash2
	git diff --no-index
	

##END git diff
--------------------------------------------------

--------------------------------------------------
##START git add

	git add fileName1 fileName2
	git add --no-all
	git add . stages all
	git add -A #stages all
	git add -u #stages modified and deleted without new
	git add -f to ignore the files

##END git add
--------------------------------------------------

--------------------------------------------------
##START git status

	git status
	git status -sb

##END git status
--------------------------------------------------

--------------------------------------------------
##START git reset
	
	git reset
	git reset --hard headnumber

## END git reset
--------------------------------------------------
--------------------------------------------------
##START git commit

	git commit -m "discription"
	git commit -am "discription"  #add and commit
	git commit -u -m "discription"
	man git commit

	-a, --all stage modified/deleted files ignoring any not "added"ed
	--ammend replace or update the last commit you made
	-o, --only commit only from paths specified on the connand line
		this disregards any staged files
		it is the default mode if a path is included ion the command line'
	-v, --verbse provides a diff in the commit message template
	-F <file>	 ude tesst from < file> as Commit message	
	-m to add message during the commit
	
##END git commit
--------------------------------------------------

--------------------------------------------------
##START git Directory

	git cat-file -p master^(tree) #

##END git 	Directory
--------------------------------------------------

--------------------------------------------------
##START Remote

	Clone #Creates a new directory, establishes upstream remote traction, checkout of active branch
	fetech #fetch will add the missing part from remote repository
	pull #fetech and merge
	
	protocals: http,ssh, git
	
	git remote add origin https://github.com/kgplrn/masterGIT.git
		
	git remote --add remoteName URL
	git remote --rm remoteName
	git remote -ename orig and newname
	git remote --show
	
	git push --set-upstream origin branchCheck

##END remote
--------------------------------------------------

--------------------------------------------------
##START remoteTrackingBranches

	git remote add  - master name url
	git remote add -t branchName name url
	git remote add -f name url

##END remoteTrackingBranches
--------------------------------------------------

--------------------------------------------------
##START Branching

	git branch newbranchName
	git checkout newbranchName
	git checkout Master

	git checkout -b hotfix

	git branch -d branchName, --delete with warning
	-D, delete without warnign

##END Branching
--------------------------------------------------

--------------------------------------------------
##START Log

	git log	
	git log --graph
	git log -nX(X is a +ve interger)
	git log --all --decorate --oneline
	//-g
	
	git log --format="%H" --since="1 week ago" --reverse | xargs -L 1 git show
##END Log
--------------------------------------------------

--------------------------------------------------
##START Merging
	
	git merge --no commit moreText
	
##END Merging
--------------------------------------------------

--------------------------------------------------
##START git rebase
	
	git rebase -i
	
	
##END git rebase
--------------------------------------------------

--------------------------------------------------
##START general  
	
	ls -la
	git ls-files -u
	
	git revert
	git blame# show the changes made by the file
	bisect
	
	git reset --hard
	git reset -soft HEAD~1
	
	git reflog -list ALL commits
	
	
	
##END general commands
--------------------------------------------------

--------------------------------------------------
##START bisect

	git bisect start
	git bisect good
	git bisect bad
	git bisect reset

##END bisect
--------------------------------------------------

--------------------------------------------------
##START patches
		
	git format-patch  master --stdout>aNewUI.patch
	git apply --stat aNewUI.patch
	
##END patches
--------------------------------------------------
--------------------------------------------------
##START git submodules
	
	git submodule init
	git submodule update
	
##END git submodules
--------------------------------------------------
--------------------------------------------------
##START stash
	
	git stash
	git stash apply stash@{1}
	git stash list
	/// save, apply, pop
	
##END stash
--------------------------------------------------
--------------------------------------------------
##START git aliases

	git config --global alias.logo "log --oneline"
	
##END git aleiases
--------------------------------------------------
--------------------------------------------------
##START GUI
	
	msysgit.github.io
	www.sourcetreeapp.com
	
	git-cola
		git-cola.github.io
	
##END GUI
--------------------------------------------------
--------------------------------------------------
##START exploration 

	git hook direcotry inside .git
	git log manual
	altering history
	git stash
	
##END exploration
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------